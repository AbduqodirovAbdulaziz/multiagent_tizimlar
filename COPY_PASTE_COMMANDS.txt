==============================================
PYTHONANYWHERE DEPLOYMENT - COPY & PASTE
==============================================

Quyidagi buyruqlarni ketma-ket copy-paste qiling:

----------------------------------------------
1. CLONE REPOSITORY
----------------------------------------------
cd /home/multiagent
git clone https://github.com/AbduqodirovAbdulaziz/multiagent_tizimlar.git
cd multiagent_tizimlar

----------------------------------------------
2. VIRTUAL ENVIRONMENT
----------------------------------------------
python3.10 -m venv venv
source venv/bin/activate

----------------------------------------------
3. INSTALL DEPENDENCIES
----------------------------------------------
pip install --upgrade pip
pip install -r requirements/base.txt

----------------------------------------------
4. MIGRATE DATABASE
----------------------------------------------
python manage.py migrate

----------------------------------------------
5. CREATE SUPERUSER
----------------------------------------------
python manage.py createsuperuser

# Username: admin
# Email: admin@example.com
# Password: (o'zingizning parolingiz)

----------------------------------------------
6. COLLECT STATIC FILES
----------------------------------------------
python manage.py collectstatic --noinput

----------------------------------------------
7. LOAD DEMO DATA
----------------------------------------------
python manage.py load_sample_data

----------------------------------------------
8. TEST
----------------------------------------------
python manage.py shell -c "from apps.patients.models import Patient; from apps.diagnostics.models import DiseasePattern; print(f'Bemorlar: {Patient.objects.count()}'); print(f'Kasalliklar: {DiseasePattern.objects.count()}')"

==============================================
PYTHONANYWHERE WEB TAB SOZLAMALARI
==============================================

Web Tab'da quyidagilarni kiriting:

1. Source code:
   /home/multiagent/multiagent_tizimlar

2. Working directory:
   /home/multiagent/multiagent_tizimlar

3. Virtualenv:
   /home/multiagent/multiagent_tizimlar/venv

4. WSGI configuration file:
   (Web Tab'da "WSGI configuration file" linkini bosing)
   
   Faylni oching va quyidagi kodni yozing:

----------------------------------------------
WSGI FILE CONTENT (copy-paste):
----------------------------------------------
import os
import sys

project_home = '/home/multiagent/multiagent_tizimlar'
if project_home not in sys.path:
    sys.path.insert(0, project_home)

os.environ['DJANGO_SETTINGS_MODULE'] = 'config.settings.dev'

venv_site_packages = '/home/multiagent/multiagent_tizimlar/venv/lib/python3.10/site-packages'
if venv_site_packages not in sys.path:
    sys.path.insert(0, venv_site_packages)

from django.core.wsgi import get_wsgi_application
application = get_wsgi_application()
----------------------------------------------

5. Static files mapping:
   URL: /static/
   Directory: /home/multiagent/multiagent_tizimlar/staticfiles

6. Media files mapping (optional):
   URL: /media/
   Directory: /home/multiagent/multiagent_tizimlar/media

7. RELOAD tugmasini bosing!

==============================================
TEKSHIRISH
==============================================

1. Web sayt: https://multiagent.pythonanywhere.com/
2. Admin: https://multiagent.pythonanywhere.com/admin/
3. API Docs: https://multiagent.pythonanywhere.com/api/docs/

==============================================
MUAMMOLARNI HAL QILISH
==============================================

Agar xatolik bo'lsa:

1. Error log'ni ko'ring (Web Tab → Error log)

2. Console'da test qiling:
   cd /home/multiagent/multiagent_tizimlar
   source venv/bin/activate
   python manage.py check

3. Static files qayta yig'ing:
   python manage.py collectstatic --noinput

4. Web Tab'da Reload qiling

==============================================
YANGILANISHLARNI DEPLOY QILISH
==============================================

Keyinchalik yangilanishlar uchun:

cd /home/multiagent/multiagent_tizimlar
source venv/bin/activate
git pull origin main
pip install -r requirements/base.txt
python manage.py migrate
python manage.py collectstatic --noinput

Web Tab'da Reload qiling.

==============================================
TAYYOR! 🚀
==============================================
